# VCS
.git
.gitignore

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / secrets — never baked into the image
.env
*.env.local

# Local embedding model cache — mounted at runtime, never in the image
docker/models/

# Node
frontend/node_modules/
frontend/dist/

# Docs & dev tooling — not needed at runtime
docs/
.claude/
.scratch/
.trae/
tests/
scripts/
*.bat
